# GUIDE — 이 워크스페이스에서 일하는 방법 (사람과 AI 모두를 위한 규칙)

> **읽는 대상:** 이 트리에 처음 붙는 AI 세션, 그리고 이 시스템을 굴리는 사람.
> **한 줄 요약:** 트리의 노드 하나가 세션 하나다. 파일을 고치기 전에 그 노드의 락을 잡고,
> 락은 항상 위→아래 순서로만 잡고, 끝나면 charter를 갱신하고 릴리스 노트를 남긴다.

이 문서는 **의도**를 담는다. 코드(`control/node.py`)만 봐서는 "왜 이렇게 했는지"를 알 수
없기 때문이다. 규칙을 바꾸려면 이 문서부터 고쳐라.

---

## 0. 대전제 — 세션이 아니라 파일이 진실이다

AI 세션은 **언제든 죽는다.** 컨텍스트 한도에 걸리고, 아카이브되고, 며칠 뒤에 열면 무슨
얘기를 했는지 아무도 모른다. 헤드리스 자동 실행은 아예 매번 **완전히 새 세션**이다.

그래서 이 시스템의 유일한 대전제는 이것이다:

> **남길 가치가 있는 것은 대화가 아니라 파일에 있어야 한다.**

세션은 소모품이다. 노드는 영속적이다. 새 세션이 charter 하나만 읽고 일을 이어받을 수
있으면 성공이고, "지난번 세션에서 얘기했잖아"가 필요하면 실패다.

노드 하나에는 성격이 다른 문서 3개가 붙는다:

| 파일 | 성격 | 규칙 |
|---|---|---|
| `<노드>.md` (**charter**) | 지금 상태 (pinned) | **고쳐 쓴다.** 낡은 문장은 지운다. 항상 현재. |
| `<노드>.log.md` (**log**) | 흐름 (append-only) | **지우지 않는다.** 한 줄씩 덧붙인다. |
| `<노드>-releases.md` (**releases**) | 학습 자료 (versioned) | 실질적 변경만. 최신이 위. |

자세한 구분과 "어디에 쓸지 헷갈릴 때" 표는 `charters/README.md`.

---

## 1. 트리 — 왜 레벨로 쪼개나

한 세션에 여러 주제를 섞으면 컨텍스트가 길어지고 판단이 둔해진다. 그래서 관심사를 레벨로
쪼개고 **세션 1개 = 노드 1개**로 고정했다. 컨텍스트는 **위→아래로만** 흐른다. 하위에서
상위에 영향을 주는 발견이 나오면 상위 charter의 「열린 질문」에 **한 줄만** 남기고 논의는
그 레벨 세션에서 한다. **하위 세션이 상위 결정을 대신 내리지 않는다.**

레벨 수는 규칙이 아니라 도구다. 아래 6단계(L0~L5)는 이 템플릿의 기본 예시이고,
개인 프로젝트라면 L4+L5만으로도 충분하다.

| 레벨 | 조직 비유 | 무엇 | 예시 charter |
|---|---|---|---|
| L0 | 세계관 / 컨트롤타워 | 트리 구조·규칙·라우팅 자체 | `charters/examples/L0-workspace.md` |
| L1 | 그룹(지주회사) | 무엇을 하며 살 것인가 / 최상위 방향 | `L1-vision.md` |
| L2 | 컨설팅펌 | 시장·산업 리서치 → 아래에 조언 | `L2-market.md` |
| L3 | 회사 / CEO | 제품군 정의, 어떤 프로젝트를 띄울지 | `L3-product-line.md` |
| L4 | 프로젝트 PM | 구체적 제품의 일상 운영 + 실무 PM | `L4-acme-app.md` |
| L5.x | 실행 채널 | 그 프로젝트 안의 개발/설계/분석 채널 | `channels/01-backend.md` |

내려갈수록 구체적·실행적이고, 올라갈수록 추상적·방향성이다. 이 비유는 트리를 **해석하는
렌즈**일 뿐이다 — charter 문서를 이 문구로 다시 쓰라는 뜻이 아니다.

**L5는 디렉터리에서 자동 발견된다** — 채널 파일(`NN-이름.md`)을 하나 만들면 노드가 하나
늘어난다. L0~L4를 추가·이동하려면 `control/tree.config.json`의 `nodes` 배열을 고친다.

### 자주 헷갈리는 경계

- **L0 vs L4:** 트리 구조·락 규칙·네이밍 = L0. 그 프로젝트의 일상 운영·배포·장애 = L4.
- **L4 vs L5:** "무엇을 언제 할지"(판단·배분) = L4. "어떻게 할지"(실행) = L5.

### 인접 레벨 통신 원칙 ★

**레벨을 건너뛰지 않는다.** 한 노드가 일을 맡기거나 보고할 수 있는 대상은 **바로 위 부모 ·
같은 레벨의 동급 · 바로 아래 자식**뿐이다.

- `L5.x → L4`: 표준. 실질적 결과/발견/결정필요 이슈를 PM에 handoff로 보고.
- `L4 → L3`: 드물게, **의미가 있을 때만.** 예: "이 제품이 잘 되니 인접 시장에 같은 걸
  하나 더 띄울 만하다" 같은 회사(L3) 차원의 신규 프로젝트 제안. 매일의 운영 얘기는 안 올린다.
- **`L5 → L0` 처럼 두 단계 이상 건너뛰는 직행 보고는 없다.** L4가 판단해서 필요하면
  자기가 한 단계 위로 다시 올린다.

*왜:* 각 레벨이 자기 바로 위/아래만 상대하면 상위 레벨이 하위의 일상 소음에 파묻히지
않는다. 상위는 "전략적으로 의미 있는 신호"만 받는다.

---

## 2. 락 — 병렬로 일하되 서로 안 밟기

여러 세션이 동시에 트리를 편집한다는 전제로 설계했다. 핵심 결정 세 가지:

### (a) 느슨형(shallow) — 락은 자기 노드 파일만 보호한다

부모 락을 쥐어도 **자식은 안 잠긴다.** 하위 파일을 건드려야 하면 그 하위 락을 **따로**
잡는다(락 2개 동시 보유가 정상이다).

*왜:* 반대(엄격형 — 부모 락이 자식 전체를 잠금)로 하면 L4 하나를 잡는 순간 채널 전부가
멈춘다. 그게 바로 피하고 싶었던 **선점 문제**다. 병렬성을 살리고, 충돌은 "실제로 같은
파일을 만질 때"만 나게 한다.

### (b) 전체 순서(total order) — 락은 항상 (레벨, 노드ID) 오름차순으로만

위→아래, 왼→오른. **이미 `L5.3`을 쥔 세션은 `L4`도 `L5.1`도 새로 잡을 수 없다**(거부된다).
둘 다 필요하면 `L4` → `L5.3` 순으로 잡아라.

*왜:* 느슨형에서는 한 세션이 락을 여러 개 쥔다. 두 세션이 서로 반대 순서로 잡으면 순환
대기 = 데드락이 생긴다. 모두가 같은 순서로만 잡으면 **순환 자체가 성립하지 않는다.**
관행이 아니라 `node.py`가 강제한다(위반 시 exit≠0).

이미 하위 락을 쥐었는데 상위가 필요해졌다면: 하위를 `release` → 상위부터 다시 잡는다.

### (c) 락은 차단 장치이자 상태판

락 파일에 **노드·레벨·세션 이름·작업 내용·시작 시각**을 적는다. 다른 세션이 `list` 한 번으로
"L4는 잡혔지만 L5.3은 비었네 → 나는 저기 붙자"를 판단할 수 있어야 하기 때문이다.
그래서 `--as "<세션 이름>"`으로 **자기가 누구인지 반드시 밝혀라.** 안 밝히면
`unnamed-session`이 되고 상태판이 무의미해진다.

### 명령

```bash
python3 control/node.py list                                   # 트리 전체 상태
python3 control/node.py --as "L4·acme·PM" claim L4 "릴리스 점검"
python3 control/node.py --as "L4·acme·PM" whoami               # 내가 쥔 락
python3 control/node.py --as "L4·acme·PM" release L4 "점검 완료"
```

### 락 규칙 요약

- **논의만 하는 세션(파일 변경 없음)은 claim 불필요.** 읽기는 막지 않는다.
- claim이 거부되면 **작업을 시작하지 말고** 누가 쥐고 있는지 사용자에게 알린다.
- 오래 방치된 락(기본 6시간, `stale_hours`)은 `list`에 ⚠️로 뜬다. **잊고 나간 락일 수
  있다** — 프로세스 생존으로는 판정할 수 없다(락 주인은 CLI 프로세스가 아니라 세션이고,
  CLI는 락을 쓰자마자 종료한다). 뺏으려면 사용자에게 먼저 확인하고 `--force`.
- 락은 어디까지나 **협력적(cooperative)** 장치다. 파일시스템 권한으로 강제하지 않는다.
  규칙을 안 지키는 세션은 막을 수 없다 — 그래서 이 문서를 세션에 읽히는 게 중요하다.

---

## 3. 우편함(handoff) — 세션끼리 대화할 수 없으니 비동기로

세션은 데몬이 아니라 **누가 턴을 줘야 깨어난다.** 그래서 세션 A가 세션 B에게 실시간으로
말을 걸 수는 없다. 대신 파일 우편함으로 넘긴다.

```bash
# 넘기기 (개발 → 검증, 설계 → 백테스트, 자식 → 부모 보고 …)
python3 control/node.py --as "L4·acme·PM" handoff L5.2 \
  "[배분/L4] 실시간 방식(폴링 vs SSE) 비교표. 배경: 대시보드 리디자인이 이 결정에 막혀 있다.
   원하는 결과: charter 「결정된 것」에 결론 + L4에 보고."

# 받기 (세션 열면 제일 먼저)
python3 control/node.py inbox L5.2
python3 control/node.py inbox            # 전체 스캔
python3 control/node.py inbox-done L5.2 1
```

설계상 중요한 점:

- **요청 원문은 절대 지우지 않는다.** 완료 처리는 체크박스만 뒤집는다(`[ ]` → `[x]`).
  "무엇을 왜 부탁받았나"가 남아야 나중에 맥락을 복원할 수 있다.
- **요청과 결과는 다른 곳에 산다.** 요청은 우편함, 결과는 charter/log/releases.
  우편함을 결과 저장소로 쓰지 마라 — 우편함은 큐일 뿐이다.
- **handoff에는 배경과 판단 근거를 충분히 적어라.** 그 세션은 나중에 열릴 때 이 텍스트만
  보고 맥락을 복원한다. 한 줄짜리 지시("이거 해줘")는 실패한다.
- **완전 무인 실행은 안 된다.** handoff는 사람에게 "그 세션을 열어라"를 알리는 것까지다
  (알림을 켰다면 텔레그램으로 온다). 진짜 무인은 스케줄러가 헤드리스 세션을 깨우는 별도
  구조가 필요하다 → `control/automation/`.

---

## 4. PM은 배분한다, 실행하지 않는다 ★

프로젝트 PM 노드(보통 L4)는 **판단 · 배분 · 취합**까지만 한다. 코드 작성·설정 변경 같은
**실행은 항상 해당 자식 노드에 handoff**한다.

*왜:* PM이 직접 실무를 하기 시작하면 세션=관심사 분리라는 전제 자체가 무너진다. PM 세션의
컨텍스트가 구현 디테일로 가득 차고, 정작 "지금 무엇이 가장 중요한가"를 판단할 여력이
없어진다. (실제로 PM 세션이 하루 종일 직접 구현·배포·버그수정까지 해버린 날이 있었고,
그날 우선순위 판단이 가장 나빴다.)

- **예외:** 지금 당장 처리해야 하는 장애/긴급 건은 직접 해도 된다. 단 **급하지 않은
  부분은 그 자리에서도 handoff로 넘긴다.**
- **사용자가 일을 주면 반드시 ①우선순위를 어떻게 매겼는지 ②누구에게 무엇을 맡겼는지를
  보고한다.** 판단 따로, 실행 따로, 보고는 항상.
- PM이 세션을 열 때의 고정 절차는 `control/session-prompts/pm-briefing.md`.

---

## 5. 버전화 원칙 ★ 우선순위 높음

이 워크스페이스에서 이뤄지는 개선은 **제품 릴리스 노트처럼 버전으로 쌓인다.** 모든 레벨에
동일하게 적용된다 — L0의 규칙 변경도, L5 채널의 알고리즘 수정도 같은 형식으로 기록한다.

**왜:**
1. 조금씩 개선되는 과정에서 **어떤 부분이 왜 나아졌는지** 남기려고.
2. 나중에 **비슷한 걸 만들 때 참고**하려고 — 과거 결정을 재사용 가능한 자산으로.
3. **과거에 뭐가 문제였는지 학습**하려고 — 실패가 사라지지 않게.

즉 이 파일들은 변경 이력이 아니라 **학습 자료**다. "무엇을 바꿨다"만 쓰면 목적의 절반을
놓친다. **왜 바꿨는지, 직전에 뭐가 문제였는지**를 반드시 함께 써라.

### 어디에 · 어떻게

노드 charter 옆에 `<charter이름>-releases.md`. 최신이 맨 위.

```bash
python3 control/node.py note L5.1 0.3.0 \
  --changed "쓰기 API에 Idempotency-Key 를 필수화하고 멱등성 처리를 미들웨어로 통합" \
  --why     "재시도 때 리소스가 중복 생성되는 사고를 구조적으로 막으려고" \
  --problem "핸들러마다 개별 구현하다 두 곳에서 누락 → 중복 생성 3건(2026-06-11)" \
  --lesson  "'모두가 지켜야 하는 규칙'은 규약이 아니라 통과 지점 한 곳으로 강제한다"
```

`--changed`는 필수, 나머지는 선택이지만 **`--why`는 사실상 필수로 취급하라.**
`--problem`/`--lesson`은 사고나 시행착오에서 나온 변경이면 반드시 채운다.

### 릴리스 노트 ≠ 일일 보고

둘은 **목적이 다르므로 절대 합치지 않는다.**

| | 릴리스 노트 | 일일/스탠드업 보고 |
|---|---|---|
| 무엇 | **실질 변경**에만 버전을 붙임 | 그날의 **활동 요약** |
| 목적 | **학습 자료** — 왜 바꿨나, 뭐가 문제였나 | 총괄 파악 — 지금 뭐가 밀렸나 |
| 누가 | 작업한 세션이 release 직전에 | `standup.py`가 자동 |
| 어디 | `<charter>-releases.md` | 알림/로그 |

**활동을 릴리스 노트에 쓰지 마라.** 매일 버전을 올리면 릴리스 노트가 활동 로그로 변질돼
학습 자료라는 목적이 죽는다. 변경이 없는 날은 릴리스 노트도 없는 게 정상이다.

### 버전 번호 (semver 차용, 문서에도 동일 적용)

- **major** (`1.0.0` → `2.0.0`) — 구조가 바뀜. 기존 방식이 더는 안 통함.
- **minor** (`0.2.0` → `0.3.0`) — 기능/규칙 추가. 기존 것은 그대로 동작.
- **patch** (`0.3.0` → `0.3.1`) — 오타·문구·작은 버그.

### 언제

**실질적 변경을 끝냈을 때 = release 직전.** 순서:

`claim` → 작업 → charter 갱신 → **`note`** → `release` → (필요시) 부모에 `handoff` 보고

`note`를 빼먹은 release는 불완전하다.

---

## 6. 버그·노하우 기록 규약

원칙: **같은 실수·같은 사고를 반복하지 않는다. 노하우는 반드시 기록되고 계속 개선된다.**
버전화 원칙(§5)이 "무엇을 개선했나"라면, 이건 "무엇이 터졌고 왜, 어떻게 피하나"를 남긴다.

1. **버그를 고치면 그 프로젝트의 `docs/BUGS.md`에 한 항목 추가한다.** 레벨 무관 —
   워크스페이스 도구(`node.py` 등)의 버그는 `control/BUGS.md`에.
2. **형식(고정):**
   ```
   ## BUG-NNN · 한 줄 제목 (YYYY-MM-DD)
   - 증상: 사용자가 본/겪은 것
   - 근본원인: 진짜 원인 (증상 말고)
   - 수정: 무엇을 어떻게 (커밋 해시)
   - 재발방지/교훈: 다음에 어떻게 피하나 / 판별법 / 재사용 포인트
   ```
   최신이 위, 번호는 계속 증가. **"무엇을 고쳤다"만 쓰면 규약 위반** — 목적은 학습이다.
3. 파일 상단에 「핵심 교훈 N줄」, 하단에 「반복 방지 체크리스트」를 유지한다.
4. 새 AI/사람은 착수 전에 그 프로젝트의 `GUIDE.md`(무엇을·어떻게) + `BUGS.md`(뭐가
   터졌나) + charter(지금 상태)를 먼저 읽는다. 이 셋이 프로젝트를 아는 최단경로다.
5. 진단 과정에서 발견한 **판별법**("이 증상은 이걸 보면 구분된다")도 교훈에 남긴다 —
   다음 진단이 빨라진다.

---

## 7. 세션 네이밍

```
L<레벨>·<노드>·<채널>        예: L0·workspace·조율, L4·acme·PM, L5·acme·backend
```

구분자는 `·`로 통일한다. 이렇게 하면 AI 도구의 세션 목록에서 상위→하위로 정렬된다.
이 이름을 그대로 `--as`에 넘겨라 — 락 상태판의 "누가"가 세션 이름과 일치해야 사람이
읽을 수 있다.

새 프로젝트(L4급)가 생기면 `L4·<프로젝트>·운영`을 먼저 만들고, 그 아래 L5 채널을 판다.

---

## 8. 새 세션이 처음 할 일 (체크리스트)

1. 사용자의 첫 마디로 **어느 노드인지** 확정한다(라우팅 표는 프로젝트 규칙 파일에).
2. 그 노드의 **charter를 읽는다.** 다른 레벨 내용을 끌어오지 않는다.
3. `node.py inbox <노드>` — 나에게 온 일이 있는지 **먼저** 확인한다.
4. `node.py list` — 지금 누가 뭘 하고 있는지 본다.
5. 코드를 건드릴 거면 그 프로젝트의 `GUIDE.md` + `BUGS.md`를 먼저 읽는다.
6. 파일을 고칠 거면 **`claim` 먼저.** 거부되면 멈추고 사용자에게 알린다.
7. 끝나면: charter 갱신 → `note` → (버그였으면 `BUGS.md`) → `release`
8. 부모가 알아야 할 완료/발견/막힘이면 `handoff <부모>` 로 보고(레벨 건너뛰기 금지).

---

## 9. 문서·가이드의 상세도 ★

> **원칙: 가이드는 항상 아주 자세하게. 더 자세한 게 낫지, 이보다 불친절하면 안 된다.**

이 워크스페이스에서 만드는 모든 사용 안내(설치·세팅·운영·인수인계 문서)는
**그 주제를 전혀 모르는 사람**을 기준으로 쓴다. "이 정도는 알겠지"가 이 시스템에서
가장 비싼 가정이다 — 읽는 쪽이 사람이면 이탈하고, AI 세션이면 추측으로 메운다.

**지킬 것:**

1. **첫 등장하는 용어는 그 자리에서 한 줄로 설명한다.** 링크로 떠넘기지 않는다.
2. **명령은 복붙 가능한 완성형으로.** `<이런 자리표시자>`를 남길 거면 무엇을 넣어야
   하는지, 그 값을 **어디서 얻는지**까지 적는다.
3. **"왜 이걸 하는지"를 먼저 한 줄.** 이유 없는 절차는 기억에 안 남고 응용도 안 된다.
4. **막히는 지점을 미리 적는다.** 실제로 사람들이 걸려 넘어진 곳(예: "봇에게 먼저
   메시지를 보내야 한다")을 증상→원인→해결로 적는다. 이게 가장 값이 큰 부분이다.
5. **검증하지 않은 명령을 사실처럼 적지 않는다.** 실행해보고 적어라. 못 해봤으면
   `⚠️ 미검증` 이라고 명시하라. 되는 척하는 문서는 없느니만 못하다.
6. **끝에 "이제 무엇을 할 수 있는가 / 다음에 뭘 하면 되는가"를 붙인다.** 설치만
   끝내고 사라지면 읽는 사람은 거기서 멈춘다.
7. **진입 경로를 하나로 명확히.** 문서가 여럿이면 "처음이면 여기부터"를 최상단에.

**AI 세션에게:** 사용자가 "가이드/설명서/사용법을 만들어달라"고 하면 위 7개를 기본값으로
적용하라. 짧게 요약해 달라는 **명시적** 요청이 없는 한, 분량을 아끼지 마라.

---

## 10. 프로젝트 고유 안전 규칙은 여기에 얹어라

트리 규칙보다 **우선하는** 도메인 규칙이 있으면 프로젝트 규칙 파일(`CLAUDE.md` 등)에 적고,
여기서는 링크만 건다. 실제 운영에서 나온 예시(형태만 참고):

- 되돌릴 수 없는 작업(DB 직접 수정, 마이그레이션) 전에는 반드시 백업 + 변경 로그.
- 외부로 나가는 커뮤니케이션(고객·투자자)과 내부 알림은 **채널을 분리**한다.
- 위험한 절차는 순서를 못박아 적는다(①…②…③…). 순서를 어겨서 사고가 났던 기록도 함께.
- 숫자를 보고할 때 "유리한 지표 하나"만 쓰지 않는다 — 기준 지표를 정하고 항상 병기.

핵심은 규칙의 내용이 아니라 **어디에 적혀 있고, 세션이 시작할 때 읽히느냐**다.
