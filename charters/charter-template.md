<!--
charter 템플릿 — 이 파일을 복사해서 노드 하나당 하나씩 만든다.
Charter template — copy this once per node.

charter = 이 노드의 "고정(pinned) 상태". 세션이 죽어도 이 파일만 읽으면 맥락이 복원된다.
          The pinned state of a node. A brand-new session reads only this and is caught up.

작성 원칙 (rules of thumb):
  1. 항상 "지금 상태"로 유지한다. 낡은 문장은 지우거나 고친다(로그가 아니다).
  2. 길이 상한을 스스로 정하라(A4 2장 정도). 넘치면 결론만 남기고 과정은 로그로 내린다.
  3. 결정은 반드시 날짜와 함께. "왜 그렇게 정했는지"가 6개월 뒤의 가치다.
  4. 다른 레벨의 내용을 끌어오지 않는다. 상위 맥락은 "상위에서 내려온 것"에 한 줄 요약만.
-->

# <노드 ID> — <노드 이름> (예: L5.1 — backend)

**성격 (Type):** <논의 전용 / 논의+구현 / 운영 / PM 등>
**한 줄 목표 (One-line goal):** <이 노드가 존재하는 이유를 한 문장으로>
**부모 노드 (Parent):** <보고·에스컬레이션 대상. 인접 레벨만>

## 목표 (Goal)

<왜 이 노드가 따로 존재하는가. 이 노드가 "성공"이라고 말할 수 있는 조건.>

## 이 노드가 다루는 것 / 다루지 않는 것 (Scope / Out of scope)

- 다룬다: <…>
- 다루지 않는다: <…> → <어느 노드로 가야 하는지 명시>

## 상위에서 내려온 것 (Inherited from parent)

<부모 노드의 원칙·제약 중 이 노드의 판단에 영향을 주는 것만 2~5줄. 원문을 복사하지
말고 요약 + 출처 경로.>

## 현재 상태 (Current state)

<지금 어디까지 와 있나. 이미 동작하는 것, 절반쯤 된 것, 알려진 한계.>

## 열린 질문 (Open questions)

1. <아직 답이 없는 것. 답이 나오면 「결정된 것」으로 옮기고 여기서 지운다(취소선도 가능).>
2. …

## 결정된 것 (Decisions)

- YYYY-MM-DD: <무엇을 정했다> — <왜 그렇게 정했나(한 줄)>

## 다음 행동 (Next actions)

- [ ] <구체적 행동. 체크박스는 스탠드업/디스패처가 스캔한다 — 형식을 지켜라>
- [ ] …

## 세션 로그 (Session log)

<한 줄 요약만. 상세 히스토리는 `<파일이름>.log.md`로.>

- YYYY-MM-DD: <이 세션에서 무슨 일이 있었나 1~2줄>
