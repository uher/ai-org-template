# INSTALL — 아무것도 모르는 상태에서 시작하기

**필요한 것: Claude 앱 하나.** 터미널을 몰라도 됩니다. 명령어를 외울 필요도 없습니다.
AI가 대화로 물어보면서 당신 전용으로 세팅해 줍니다.

> 이미 개발 환경이 익숙하고 명령어로 직접 하고 싶다면 → [`QUICKSTART.md`](QUICKSTART.md) (10분)

---

## 1. Claude 앱 설치

[claude.ai/download](https://claude.ai/download) 에서 받아 설치하고 로그인합니다.
(Mac / Windows 모두 있습니다.)

## 2. 왼쪽 위 `</> Code` 를 누릅니다

앱을 열면 사이드바 맨 위에 **홈** 과 **`</> Code`** 탭이 있습니다.
**`</> Code`** 를 누르세요. 이 템플릿은 파일을 만들고 명령을 실행해야 하므로
일반 채팅이 아니라 Code 쪽에서 써야 합니다.

## 3. `+ 새로 생성` 을 누릅니다

프로젝트를 둘 폴더를 물어보면, 새로 만들거나 빈 폴더를 고르세요.
(예: `~/work` 또는 `문서/내작업`)

## 4. 아래 문장을 그대로 붙여넣습니다

```
https://github.com/uher/ai-org-template 이 저장소를 받아서 의도대로 세팅해줘.
```

이게 전부입니다. 이제부터는 Claude가 안내합니다.

## 5. Claude가 물어보는 것에 답하면 됩니다

순서대로 이런 걸 물어봅니다. **모르면 "잘 모르겠다"고 하거나 "나중에"라고 하면 넘어갑니다.**

| 순서 | 무엇을 묻나 | 왜 |
|---|---|---|
| ① | 직업 / 하는 일 / 굴리는 프로젝트 | 당신 일에 맞는 작업 구조(트리)를 짜기 위해 |
| ② | 텔레그램 봇 토큰 | 알림을 받기 위해 (아래 참고) |
| ③ | 매일 챙겨볼 관심 주제 | 관련 뉴스·정보를 모아 요약해 보내주기 위해 |
| ④ | 쓰는 AI 도구 | 앞으로 여는 세션이 규칙을 자동으로 읽게 하려고 |

### ②번 — 텔레그램 봇 만들기 (3분)

Claude가 이 순서를 다시 알려주지만, 미리 해두면 빠릅니다.

1. 텔레그램 앱에서 **@BotFather** 를 검색해 대화를 시작합니다
2. `/newbot` 을 보냅니다
3. 봇 **이름**을 정합니다 (아무거나. 예: `내 비서`)
4. 봇 **사용자명**을 정합니다 — **반드시 `_bot` 으로 끝나야 합니다** (예: `hong_jarvis_bot`)
5. `123456789:AAE...` 형태의 **토큰**을 줍니다 → 이걸 Claude에게 붙여넣으세요
6. **⚠️ 그리고 방금 만든 봇을 찾아가 아무 메시지나 한 번 보내세요.**
   이걸 빠뜨리면 봇이 당신에게 먼저 말을 걸 수 없습니다. **가장 흔한 실패 원인입니다.**

> 토큰은 비밀번호와 같습니다. Claude는 이 값을 `control/notify.conf` (권한 600,
> git에 올라가지 않음)에만 저장하고 화면에 다시 띄우지 않습니다.
> 공식 안내: https://core.telegram.org/bots/features#creating-a-new-bot

### ③번 — 관심사 브리핑은 돈이 듭니다

매일 뉴스를 검색·요약하려면 AI 세션이 자동으로 깨어나야 하고, **실행할 때마다 비용이
발생합니다**(1회당 대략 몇십 센트~몇 달러). Claude가 예산 상한 거는 법을 함께 알려줍니다.
부담되면 건너뛰고 나중에 켜도 됩니다.

---

## 세팅이 끝나면 무엇을 할 수 있나

이 템플릿은 **여러 개의 AI 세션을 각자 다른 일을 맡은 팀처럼 굴리는 방법**입니다.
한 대화에 전략·설계·구현·운영을 다 밀어넣으면 판단이 흐려지고, 대화가 끝나면
결정이 통째로 사라집니다. 그래서 일을 **레벨로 나누고**, 상태를 **대화가 아니라 파일에**
둡니다. 세션은 소모품이 되고, 파일이 진실이 됩니다.

세팅 후 할 수 있는 것:

- **개인 비서처럼 쓰기** — 생각나는 할 일을 계속 던져두고, 아침엔 "오늘 뭘 해야 하나",
  저녁엔 "오늘 뭐가 됐나"를 텔레그램으로 받습니다.
- **프로젝트를 병렬로 굴리기** — 산업/제품/프로젝트/채널로 나눠 세션을 여러 개 띄웁니다.
  같은 파일을 동시에 건드리지 않게 **락**이 막아줍니다.
- **세션끼리 일 넘기기** — 세션은 실시간으로 대화하지 못합니다. 대신 **우편함**에
  일을 남기고, 텔레그램으로 "그 세션 열어서 처리하세요" 알림이 갑니다.
- **결정의 이유를 남기기** — 릴리스 노트에 *무엇을·왜·직전에 뭐가 문제였나·교훈*을
  적습니다. 6개월 뒤 같은 실수를 반복하지 않기 위한 장치입니다.
- **일부 자동화** — 스케줄러가 헤드리스 세션을 깨워 밀린 작업 하나를 처리하게 할 수
  있습니다. (한계는 아래)

## 솔직하게 — 한계

- **완전 무인은 안 됩니다.** 알림은 "당신이 그 세션을 여세요"까지입니다.
  헤드리스 자동화를 붙여도 그건 매번 기억 없는 새 세션이라, 맥락은 전부 파일에서 옵니다.
- **유지비가 듭니다.** charter를 최신으로 고치고, 릴리스 노트를 성실히 쓰고, 락을 잊지
  않고 풀어야 합니다. **그 습관이 안 붙으면 파일만 늘고 신뢰는 줍니다.**
- **작은 일에는 과합니다.** 주말 프로젝트라면 `TODO.md` 하나가 낫습니다.
  이건 여러 일을 오래, 병렬로 굴릴 때 값을 합니다.

---

## 막혔을 때

| 증상 | 해결 |
|---|---|
| 텔레그램 테스트 메시지가 안 옴 | 봇에게 **당신이 먼저** 메시지를 보냈나요? 그게 대부분의 원인입니다 |
| `python3: command not found` | Mac: `brew install python3` · Windows: [python.org](https://www.python.org/downloads/) |
| `getUpdates` 결과가 비어 있음 | 같은 이유입니다. 봇에게 아무 메시지나 보낸 뒤 다시 하세요 |
| 세팅하다 중간에 끊김 | 새 세션을 열고 "`SETUP-STATE.md` 보고 이어서 세팅해줘" 라고 하세요 |
| 뭘 해야 할지 모르겠음 | `python3 control/node.py list` 로 현재 상태를 보고, Claude에게 "다음에 뭘 하면 될까?" 라고 물으세요 |

## 다음에 읽을 것

- [`README.md`](README.md) — 왜 이렇게 만들었나, 나에게 맞는 도구인가
- [`GUIDE.md`](GUIDE.md) — 규칙 전문 (AI 세션이 읽는 문서)
- [`QUICKSTART.md`](QUICKSTART.md) — 명령어로 직접 하고 싶을 때
- [`SETUP.md`](SETUP.md) — 기존 프로젝트에 붙이기, 고급 자동화
